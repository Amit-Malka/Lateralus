/**
 * Content script — injected into matching pages (LinkedIn, Indeed, etc.)
 *
 * Use this to scrape job data, inject UI overlays, or
 * communicate with the background service worker.
 */

console.log("[Job Hunter AI] Content script loaded on", window.location.href);
